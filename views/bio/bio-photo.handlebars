<h6>Profile</h6>
<div class="card card-body">
    <h2 class="light">{{user.name}}</h2>
    <br>
    <h4>My Jobs</h4>
    <br>
    {{#each jobs}}
        <button class="btn btn-light mb-2 bio-client-button"><a class="bio-client-anchor" href="http://localhost:5000/jobs"> {{title}} | {{startdate}} | {{starttime}}</a></button>
    {{/each}}
    <h4>My Portfolio</h4>
    <p class="bg-light text-dark">Hi, here are my photography works! I hope you are enjoying them!</p>
    {{#each user.portfolio}}
        <img class="portfolio mb-2" src="..{{this}}" alt="image">
        {{#if (isCurrentUser ../user.id ../user.compareID)}}
        <form method="post" action="/users/{{../user.id}}?_method=PUT">
            <input type="hidden" name="_method" value="PUT">
            <button type="submit" class="btn btn-danger btn-block mb-2">Delete</button>
        </form>
        {{/if}}
        <br>
    {{/each}}
    <br>
    {{#if (isCurrentUser user.id user.compareID)}}
        <h4>Upload An Image</h4>
        {{!-- upload images to my portfolio --}}
        <form method="post" enctype="multipart/form-data" action="/users/uploads/{{user._id}}">
        <input type="file" name="file" id="file" class="inputfile mb-2">
        <label for="file">Choose a file</label>
        <br>
        <input type="submit" value="Submit" class="btn btn-primary border-dark">
    </form>
    {{/if}}
<br>
        <button class="btn btn-light border-dark"> <a href="/jobs" style=" text-decoration: None">Go Back</a></button>
</div>